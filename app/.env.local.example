# Copy this file to app/.env.local.
# This value is public and is compiled into the browser bundle during next build.
# Use the backend HTTP(S) origin without a trailing slash.
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000

# Never put provider credentials, ADC JSON, or any other secret here.
